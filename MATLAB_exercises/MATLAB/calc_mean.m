function meanIM = calc_mean(tfStack)

% The image stack has three dimensions: x, y, frame
% Use the 'mean' command to compute the average of the stack, along the
% frame dimension, and return the result in a variable 'meanIM'.

